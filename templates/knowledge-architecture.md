---
name: architecture-{{SYSTEM}}
description: >-
  AS-IS architecture of system {{SYSTEM}} (arc42 + C4). Authoritative,
  code-derived context for agents. Read this BEFORE re-indexing or re-reading
  the repositories.
generado_desde:
{{SEALS_FRONTMATTER}}
verificado: {{FECHA}}
---

# AS-IS architecture — system **{{SYSTEM}}**

> **Standard:** arc42 structure · C4 model diagrams (Context / Container /
> Component) in Mermaid, readable by humans and by agents.
> **[{{GEN_TAG}} · {{FECHA}}]** Generated by `scripts/generate-architecture.sh`
> after indexing, from the topology (`repos.yaml`), the as-is map
> (`knowledge/as-is/`), the indexed code graph and the context packs.
> Origin seals: {{SEALS_INLINE}}.
>
> **Usage policy (documented context first):** this document and the context
> packs `{{PREFIX}}-*` are the source of context to work on {{SYSTEM}}.
> **Do NOT re-index or re-read the code** for what is already documented here.
> Consult the code (or the graph) **only** for **undocumented** questions that
> the human cannot answer, or when more detail is needed; if you find something
> new, **update it here** and turn it into an assertion
> (`scripts/assertions.d/{{SYSTEM}}.sh`).

---

## 1. Introduction and goals
<!-- NARRATIVE: INTRO -->

## 2. Constraints
<!-- NARRATIVE: CONSTRAINTS -->

## 3. Context and scope (C4 Level 1)
<!-- NARRATIVE: CONTEXT -->

## 4. Solution strategy
<!-- NARRATIVE: STRATEGY -->

## 5. Building block view (C4 Levels 2–3)

### 5.1 System repositories (topology derived from the registry)
{{REPOS_TABLE}}

### 5.2 Code metrics (derived at indexing time)
{{METRICS_TABLE}}

<!-- NARRATIVE: BUILDING_BLOCKS -->

## 6. Runtime view
<!-- NARRATIVE: RUNTIME -->

## 7. Deployment view
<!-- NARRATIVE: DEPLOYMENT -->

## 8. Crosscutting concepts
<!-- NARRATIVE: CROSSCUTTING -->

## 9. Architecture decisions
<!-- NARRATIVE: DECISIONS -->

## 10. Quality requirements
<!-- NARRATIVE: QUALITY -->

## 11. Risks and technical debt
<!-- NARRATIVE: RISKS -->

## 12. Glossary
<!-- NARRATIVE: GLOSSARY -->

---

## Annex A · Complete endpoint inventory
<!-- NARRATIVE: ENDPOINTS -->

## Annex B · Complete dependencies (derived)
{{DEPS_TABLES}}

<!-- NARRATIVE: DEPS_NOTE -->

## Annex C · Validation and regeneration
- **Regenerate:** `./scripts/generate-architecture.sh` (also runs at the end of
  `./scripts/generate-as-is.sh`, i.e. after every indexing with `/repo-add`).
  This `.md` file and its `.html` twin are rebuilt from the curated narrative
  (`{{NARRATIVE_FILE}}`) plus the data derived from the code.
- **Validate:** `scripts/assertions.sh {{SYSTEM}}` · `scripts/freshness.sh check`.
- **Agent consumption:** the `{{PREFIX}}-architecture` context skill (triggered by
  the system's repo names) points at this document.
<!-- NARRATIVE: VALIDATION -->
