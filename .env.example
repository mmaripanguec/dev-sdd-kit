# Credenciales Bitbucket para setup.sh (copiar a .env — el .env esta gitignoreado)
#   cp .env.example .env && chmod 600 .env
#
# Formato shell: KEY=valor, sin espacios alrededor del '='.
#
# BITBUCKET_USER depende del TIPO de token que uses:
#   - App Password            -> tu usuario Bitbucket        (ej: mmaripanguec)
#     crear en: bitbucket.org > Personal settings > App passwords
#     permiso minimo: Repositories: Read (Write si haras push)
#   - API token de Atlassian  -> tu email de la cuenta       (ej: name@example.com)
#     crear en: id.atlassian.com > Security > API tokens
#   - Access Token de repo/proyecto/workspace -> x-token-auth
#     crear en: Repository/Workspace settings > Access tokens

# NOTA IMPORTANTE - API tokens CON scopes (ATATT de ~190+ caracteres):
#   la API REST los acepta con tu email, pero GIT exige el usuario especial
#   x-bitbucket-api-token-auth. setup.sh v5 lo aplica solo; si necesitas
#   forzarlo manualmente, descomenta:
# BITBUCKET_GIT_USER=x-bitbucket-api-token-auth

BITBUCKET_USER=user@example-bank.com
BITBUCKET_TOKEN=pega_aqui_tu_token
